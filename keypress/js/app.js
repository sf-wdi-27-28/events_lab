console.log("Sanity Check: JS is working!");

$(document).ready(function() {
// I'll try to finish this in the morning and submit another pull request,
// but I believe the psuedo code would be something like monitoring the timestamp
// on first keypress. Then on the next keypress it takes the new timestamp,
// finds the difference in time, and then displays it in the span element.
// I'll try to see if I can code this tomorrow.
});
